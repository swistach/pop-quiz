export enum ButtonSize {
  Small = 'sm',
  Medium = 'md',
  Large = 'lg',
}

export enum ButtonVariant {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum ButtonDisplay {
  Block = 'block',
  InlineBlock = 'inline-block',
}
